/**
 * Legacy entry — the LangGraph advisor calls `extractCredGenieProfile` directly.
 * Kept so older imports keep working.
 */
export { extractCredGenieProfile as extractAdvisorIntent } from "./extractProfile";
